server:
  port: 13011

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:13001/eureka/

spring:
  application:
    # 根据 name进行注册和访问
    name: client-ac